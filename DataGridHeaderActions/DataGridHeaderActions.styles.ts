import styled from 'styled-components';

export const ActionsContainer = styled.th`
    position: sticky;
    z-index: 300;
    right: 0;
    top: 0;
    padding: 0 21px;
    background: linear-gradient(90deg, rgba(255, 255, 255, 0) 0%, rgb(255, 255, 255) 100%);
`;
